#!/usr/bin/env python

from distutils.core import setup

setup(name='snowcast',
      version='0.0.1',
      description='SnowCast wormhole workflow includes all the code for individual steps',
      author='SnowCast',
      author_email='admin@snowcast.com',
      url='http://www.snowcast.com',
      packages=['code'],
     )